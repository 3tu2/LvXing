# ============================================================
# 根目录 Git 忽略规则
# 说明:后端、前端各自目录里还有更细的 .gitignore,
# 这里只放"项目根这一层"不该提交的东西。
# ============================================================

# Claude Code 的本地配置/会话状态(与项目代码无关)
.claude/

# 操作系统自动生成的文件
.DS_Store
Thumbs.db
