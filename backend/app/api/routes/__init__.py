"""API路由模块。

"路由"就是"网址(URL)和它对应的处理函数"之间的映射关系。
比如当用户访问 GET /api/map/poi 时,FastAPI 就会调用 map.py 里对应的函数。

这个文件夹里的每个 .py 文件负责一组相关的接口:
- trip.py:旅行规划相关接口
- poi.py:POI(兴趣点/景点)相关接口
- map.py:地图服务相关接口(搜索、天气、路线)

`__init__.py` 让这个文件夹成为 Python 包。
"""
