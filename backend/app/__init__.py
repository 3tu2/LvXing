"""LangGraph智能旅行助手 - 后端应用(LangChain + LangGraph)。

这个文件是一个"包标识文件"(package marker)。

小白可以这样理解:
在 Python 里,一个文件夹里只要有名为 __init__.py 的文件,
这个文件夹就会被当成一个"包"(package),可以用 `from app.xxx import yyy` 的方式导入里面的模块。
这里还顺便定义了整个应用的版本号,方便别处引用 `app.__version__`。
"""

__version__ = "1.0.0"
